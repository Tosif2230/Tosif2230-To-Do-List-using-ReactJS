# To-Do App (React)
Simple ToDo Application To manage daily tasks.
built using React and Tailwind CSS.
User can add tasks, mark them as complete, edit them inline, and delete them.

# Features
    * Add new tasks
    * Mark tasks as completed
    * Edit and Update tasks inside the list
    * Delete tasks
